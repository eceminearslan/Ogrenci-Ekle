 @model (List<Bolum> bolums, List<Ogrenci> ogrencis)

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <title>Vize</title>
    <link rel="icon" type="image/x-icon" href="assets/favicon.ico" />
    <link href="css/styles.css" rel="stylesheet" />
</head>

<body>
    <div class="d-flex" id="wrapper">
        <!-- Sidebar-->
        <div class="border-end bg-white" id="sidebar-wrapper">
            <div class="sidebar-heading border-bottom bg-light">Bölümler</div>
            <div class="list-group list-group-flush">
                @foreach (var item in Model.bolums)
                {
                    <a class="list-group-item list-group-item-action list-group-item-light p-3" asp-action="Index" asp-controller="Home" asp-route-id="@item.BolumId">@item.BolumAd</a>
                }
            </div>
        </div>
        <!-- Page content wrapper-->
        <div id="page-content-wrapper" class="float-right">

            <table class="table table-striped">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Bölüm Adı</th>
                        <th scope="col">Ad</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.ogrencis)
                    {
                        <tr>
                            <td scope="row">@item.OgrenciId</td>
                            <td>@item.Bolum.BolumAd</td>
                            <td>@item.OgrenciAd @item.OgrenciSoyad</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        </nav>
    </div>
    </div>
    <!-- Bootstrap core JS-->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Core theme JS-->
    <script src="js/scripts.js"></script>
</body>

</html> 







@* @model (List<Bolum> bolums, List<Ogrenci> ogrencis)

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <title>Vize</title>
    <link rel="icon" type="image/x-icon" href="assets/favicon.ico" />
    <link href="css/styles.css" rel="stylesheet" />
</head>
<body>
    <div class="d-flex" id="wrapper">
        <!-- Sidebar-->
        <div class="border-end bg-white" id="sidebar-wrapper">
            <div class="sidebar-heading border-bottom bg-light">Bölümler</div>
            <div class="list-group list-group-flush">
                @foreach (var item in Model.bolums)
                {
                    <a class="list-group-item list-group-item-action list-group-item-light p-3" asp-action="Index" asp-controller="Home" asp-route-id="@item.BolumId">@item.BolumAd</a>
                }
            </div>
            <!-- Ders Ara Formu -->
            <div class="mt-3 p-3">
                <h3>Ders Ara</h3>
                @using (Html.BeginForm("Ara", "Ders", FormMethod.Post))
                {
                    <p>
                        Ders Adı: @Html.TextBox("dersAdi")
                        <input type="submit" value="Ara" class="btn btn-primary" />
                    </p>
                }
            </div>
        </div>
        <!-- Page content wrapper-->
        <div id="page-content-wrapper" class="float-right">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Bölüm Adı</th>
                        <th scope="col">Ad</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.ogrencis)
                    {
                        <tr>
                            <td scope="row">@item.OgrenciId</td>
                            <td>@item.Bolum.BolumAd</td>
                            <td>@item.OgrenciAd @item.OgrenciSoyad</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    <!-- Bootstrap core JS-->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Core theme JS-->
    <script src="js/scripts.js"></script>
</body>
</html> *@




@* @model (List<Bolum> bolums, List<Ogrenci> ogrencis)

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <title>Vize</title>
    <link rel="icon" type="image/x-icon" href="assets/favicon.ico" />
    <link href="css/styles.css" rel="stylesheet" />
</head>
<body>
    <div class="d-flex" id="wrapper">
        <!-- Sidebar-->
        <div class="border-end bg-white" id="sidebar-wrapper">
            <div class="sidebar-heading border-bottom bg-light">Bölümler</div>
            <div class="list-group list-group-flush">
                @foreach (var item in Model.bolums)
                {
                    <a class="list-group-item list-group-item-action list-group-item-light p-3" asp-action="Index" asp-controller="Home" asp-route-id="@item.BolumId">@item.BolumAd</a>
                }
            </div>
        </div>
        <!-- Page content wrapper-->
        <div id="page-content-wrapper" class="float-right">
            <!-- Navigation Bar -->
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <a class="navbar-brand" href="#">Anasayfa</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index", "Home")">Anasayfa</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("DersEkle", "Ders")">Ders Ekle</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("OgrenciEkle", "Ogrenci")">Öğrenci Ekle</a>
                        </li>
                    </ul>
                    <!-- Ders Ara Formu -->
                    @using (Html.BeginForm("Ara", "Ders", FormMethod.Post, new { @class = "form-inline my-2 my-lg-0" }))
                    {
                        @Html.TextBox("dersAdi", null, new { @class = "form-control mr-sm-2", @placeholder = "Ders Adı" })
                        <input type="submit" value="Ara" class="btn btn-outline-success my-2 my-sm-0" />
                    }
                </div>
            </nav>
            <div class="container-fluid">
                <table class="table table-striped mt-3">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Bölüm Adı</th>
                            <th scope="col">Ad</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.ogrencis)
                        {
                            <tr>
                                <td scope="row">@item.OgrenciId</td>
                                <td>@item.Bolum.BolumAd</td>
                                <td>@item.OgrenciAd @item.OgrenciSoyad</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <!-- Bootstrap core JS-->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Core theme JS-->
    <script src="js/scripts.js"></script>
</body>
</html> *@




@* @model (List<Bolum> bolums, List<Ogrenci> ogrencis)

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <title>Vize</title>
    <link rel="icon" type="image/x-icon" href="assets/favicon.ico" />
    <link href="css/styles.css" rel="stylesheet" />
</head>

<body>
    <div class="d-flex" id="wrapper">
        <!-- Sidebar-->
        <div class="border-end bg-white" id="sidebar-wrapper">
            <div class="sidebar-heading border-bottom bg-light">Bölümler</div>
            <div class="list-group list-group-flush">
                @foreach (var item in Model.bolums)
                {
                    <a class="list-group-item list-group-item-action list-group-item-light p-3" asp-action="Index" asp-controller="Home" asp-route-id="@item.BolumId">@item.BolumAd</a>
                }
            </div>
        </div>
        <!-- Page content wrapper-->
        <div id="page-content-wrapper" class="float-right">
            <!-- Navigation Bar -->
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <!-- Sol Üst Köşe -->
                <a class="navbar-brand" href="#">Vize</a>
                <!-- Ders Ara Formu -->
                @using (Html.BeginForm("Ara", "Ders", FormMethod.Post, new { @class = "form-inline ml-auto" }))
                {
                    @Html.TextBox("dersAdi", null, new { @class = "form-control mr-sm-2", @placeholder = "Ders Adı" })
                    <input type="submit" value="Ara" class="btn btn-outline-success my-2 my-sm-0" />
                }
                <!-- Sol Üst Köşe Bitiş -->
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Index", "Home")">Anasayfa</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("DersEkle", "Ders")">Ders Ekle</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("OgrenciEkle", "Ogrenci")">Öğrenci Ekle</a>
                        </li>
                    </ul>
                </div>
            </nav>
            <div class="container-fluid">
                <table class="table table-striped mt-3">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Bölüm Adı</th>
                            <th scope="col">Ad</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.ogrencis)
                        {
                            <tr>
                                <td scope="row">@item.OgrenciId</td>
                                <td>@item.Bolum.BolumAd</td>
                                <td>@item.OgrenciAd @item.OgrenciSoyad</td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
    <!-- Bootstrap core JS-->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Core theme JS-->
    <script src="js/scripts.js"></script>
</body>

</html> *@





